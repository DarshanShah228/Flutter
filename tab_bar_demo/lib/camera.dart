import 'package:flutter/material.dart';

class carmera extends StatelessWidget {
  const carmera({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text("Camera"),
        ],
      ),
    );
  }
}
